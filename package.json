{
  "name": "gamengine",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:shared": "tsc -p packages/shared/tsconfig.json",
    "build:server": "tsc -p packages/server/tsconfig.json",
    "build":        "npm run build:shared && npm run build:server",
    "typecheck":    "npm run typecheck -w packages/client",
    "test":         "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
